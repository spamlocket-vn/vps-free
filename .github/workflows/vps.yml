name: 5 VPS Ubuntu

on:
  workflow_dispatch:

jobs:
  vps1:
    runs-on: ubuntu-latest
    steps:
      - name: Install tmate (VPS1)
        run: |
          sudo apt update
          sudo apt install -y tmate
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "🔑 VPS1 SSH:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
          echo "🌐 VPS1 Web:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_web}'
      - name: Keep alive
        run: sleep 6h

  vps2:
    runs-on: ubuntu-latest
    steps:
      - name: Install tmate (VPS2)
        run: |
          sudo apt update
          sudo apt install -y tmate
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "🔑 VPS2 SSH:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
          echo "🌐 VPS2 Web:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_web}'
      - name: Keep alive
        run: sleep 6h

  vps3:
    runs-on: ubuntu-latest
    steps:
      - name: Install tmate (VPS3)
        run: |
          sudo apt update
          sudo apt install -y tmate
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "🔑 VPS3 SSH:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
          echo "🌐 VPS3 Web:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_web}'
      - name: Keep alive
        run: sleep 6h

  vps4:
    runs-on: ubuntu-latest
    steps:
      - name: Install tmate (VPS4)
        run: |
          sudo apt update
          sudo apt install -y tmate
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "🔑 VPS4 SSH:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
          echo "🌐 VPS4 Web:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_web}'
      - name: Keep alive
        run: sleep 6h

  vps5:
    runs-on: ubuntu-latest
    steps:
      - name: Install tmate (VPS5)
        run: |
          sudo apt update
          sudo apt install -y tmate
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "🔑 VPS5 SSH:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
          echo "🌐 VPS5 Web:"
          tmate -S /tmp/tmate.sock display -p '#{tmate_web}'
      - name: Keep alive
        run: sleep 6h
